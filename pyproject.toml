[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "puzzle_solver"
version = "0.1.0"
description = "Fast A* solver for 8-puzzle using Rust"
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3/extension-module"]

